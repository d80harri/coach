apply plugin: 'java'
apply plugin: 'eclipse'

dependencies {
    //compile project(":domain-interfaces")

    compile "org.hibernate:hibernate-core:4.1.4.Final" // TODO: hibernate and flyway should not be dependencies in an interface project
    compile ('org.flywaydb:flyway-core:4.0')
}